# Legacy (not maintained)

Superseded by the HTML frontend in `frontend/`. Kept for history only.

- `streamlit_app.py` — original Streamlit UI (needs `pip install streamlit`)
- `cli_main.py` — Phase-1 CLI runner
- `weapons_tool.py` — old Wikipedia-scraping weapons tool used only by the Streamlit UI
- `test_cache.py` — ad-hoc Mongo cache inspection script
